# Torrent Stream Server Configuration

# Node environment
NODE_ENV=production

# Server port
PORT=3000

# Node.js memory allocation (in MB)
NODE_OPTIONS=--max-old-space-size=2048

# Optional: API timeout (ms)
API_TIMEOUT=30000

# Optional: Enable verbose logging
DEBUG=false

# Optional: Temporary streams directory
STREAMS_DIR=/tmp/streams

# Optional: Maximum concurrent streams
MAX_STREAMS=5

# Optional: Stream segment duration (seconds)
SEGMENT_DURATION=4

# Optional: HLS playlist size (number of segments)
HLS_PLAYLIST_SIZE=10

# Optional: Peer connection timeout (ms)
PEER_TIMEOUT=10000
